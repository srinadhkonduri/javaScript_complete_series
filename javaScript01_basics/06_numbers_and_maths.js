const score = 400
console.log(score)

const balance = new Number(400)
console.log(balance)

// used to print the length of the string
console.log(balance.toString().length)

// used to assing the value in a number after the number ends
console.log(balance.toFixed(2))

// used to precise a value for a number and print the number
const otherNumber = 34.748934
console.log(otherNumber.toPrecision(6))


// converts a number into indian reading technique
const aValue = 100000000
console.log(aValue.toLocaleString('en-IN'))


// ****************** MATHS ******************

// prints an object that is empty
console.log(Math)

// prints a random value and multiply by 100
console.log(Math.random()*100)

// prints the min value in the given numbers
console.log(Math.min(4,3,5,1))

// prints the maximum numbers in the given numbers
console.log(Math.max(4,78,65,90))